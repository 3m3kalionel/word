{
  "name": "word",
  "version": "1.0.0",
  "description": "a word function that count the number of words in a string and returns an object of key/value - word/wordcount pairs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jasmine": "^2.6.0"
  }
}
